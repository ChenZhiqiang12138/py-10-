main
{
	主进程依次开启三个进程（必须主进程开始三个，一生二，二生四的话子进程会和主进程竞争）
	建立两个内存映射，分别保存标签为0和标签为1的数据各个特征的总和
	建立一个消息数组，用于进程同步
	if()通过进程的pid来判断当前是哪一个进程
	{
		读取数据，一个进程读1800行，读到内存映射里（分0,1读）
		记录0,1的数量
		while死循环等待进程同步
		计算各个特征的均值
		读取测试数据（计算到0标签和1标签的欧式距离）直接预测
	}
}